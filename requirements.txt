# Pinned exactly for reproducible builds: generate_icons.py rasterises the
# launcher icons through CairoSVG and Pillow, and their PNG output can change
# between versions. See docs/reproducible-builds.md.
cairosvg==2.9.0
pillow==12.3.0
requests==2.34.2
